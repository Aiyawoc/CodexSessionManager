# Codex Session Management

CodexSessionManager 管理 Codex 任务的可审计生命周期：读取官方 App Server 投影、生成不可变计划、建立可恢复证据，并在安全门禁复核后执行受控写入。

## Language

**任务（Task）**：
Codex App Server 暴露的一个可独立读取、派生和归档的对话单元。
_避免_：session、chat、conversation（面向用户时统一称“任务”）

**任务快照（Task Snapshot）**：
在某一时刻规范化的任务内容、管理状态与关系信息。
_避免_：原始 JSONL、数据库记录

**后代闭包（Descendant Closure）**：
一个根任务及所有通过父子或派生关系可达的后代任务集合。
_避免_：仅当前选中项、直接子任务列表

**能力矩阵（Capability Matrix）**：
由本地 Codex 二进制、初始化响应和精确 schema 共同确定的 App Server 能力快照。
_避免_：版本号推测、功能开关字典

**协议画像（Protocol Profile）**：
经人工审计批准的精确 Codex 版本、schema 哈希、方法清单和关键字段组合。
_避免_：兼容版本范围、自动信任规则

**计划（Plan）**：
绑定能力、目标闭包、状态与内容指纹、有效期和动作参数的不可变写入意图。
_避免_：命令参数、待办事项

**备份复验证据（Backup Verification Evidence）**：
完整解密并校验 `.csmbackup` 后，与来源任务备份指纹绑定的审计记录。
_避免_：文件存在、压缩包可打开

**可信归档（Trusted Archive）**：
由 CSM 计划执行或原生归档对账建立，且绑定已复验备份的归档状态。
_避免_：任意已归档任务

**裁剪投影（Trim Projection）**：
由 `keep`、`exclude`、`summary` 和 `protect` 选择计算出的模型可见上下文。
_避免_：修改后的原历史

**派生任务（Derived Task）**：
承载裁剪投影的新 Codex 任务，来源任务保持不变。
_避免_：覆盖、压缩原任务

**验收报告（Acceptance Report）**：
只包含散列标识、证据哈希和固定阶段结果的脱敏人工验收摘要。
_避免_：对话内容转储、生产认证

## Relationships

- 一个**能力矩阵**至多匹配一个获批**协议画像**；未精确匹配时所有写能力关闭。
- 一个**计划**绑定一个能力矩阵指纹，并覆盖一个或多个互不重叠的**后代闭包**。
- 一份**备份复验证据**覆盖一个或多个完整**任务快照**，归档计划执行前必须重新匹配来源指纹。
- 一个**裁剪投影**由一个 TrimPlan 和一个来源任务快照产生，并只能写入新的**派生任务**。
- 一个**可信归档**必须能追溯到计划、备份清单和审计链；普通归档状态本身不是删除门禁。
- 一份**验收报告**引用 schema 报告、计划、备份和审计哈希，但不包含任务原始 ID 或内容。

## Example dialogue

> **开发者：** “用户选了一个根任务，可以只读取这个任务就生成归档计划吗？”
> **领域专家：** “不能只读根任务。先用摘要索引解析完整后代闭包，再只深读闭包中的任务快照；计划必须绑定整个闭包和当前能力矩阵。”
>
> **开发者：** “App Server 多了一个方法，能自动加入写入白名单吗？”
> **领域专家：** “不能。先生成 schema 审计报告，由人工审查新增、移除、稳定性和关键字段差异；未知画像继续只读。”

## Flagged ambiguities

- “session” 既曾指 Codex 任务，也曾指一次 App Server 连接；已解决：领域对象称**任务**，传输生命周期称“连接”。
- “验证通过”可能只表示单元测试、offscreen GUI、bundle 或真实账号联调；已解决：报告必须明确证据层级，不使用无范围的“E2E/生产验收”。
- “备份完成”可能只表示文件已生成；已解决：安全门禁中的完成必须是**备份复验证据**已写入审计链。
- “归档任务”可能指 App 中任意 archived 状态；已解决：永久删除只接受**可信归档**。
