# CodexSessionManager v1.1.0 首次交付候选

状态：**源码与本地 ad-hoc macOS arm64 测试包已通过首次交付自动门禁，尚未公开发布**。本版本可以交给首批用户进行受控测试，但不宣称生产就绪、Developer ID 签名/公证完成或跨平台正式发布。

## 首次交付范围

### 对话清理

- 本地规则建立安全候选池，LLM/Skill/MCP 只能排序和解释候选；
- 候选灌入原项目/任务 GUI，由用户取消或补选；
- 展示项目、后代闭包、最后活动时间、大小、风险、建议理由和备份状态；
- “备份并归档”先创建并完整复验 age 加密备份，再重新读取状态、能力、建议指纹和后代闭包，最后执行归档；
- 永久删除保持独立人工高风险流程，不进入自动清理链路。

### 上下文优化与待处理计划

- LLM 建议绑定当前 turn/item ID 和内容指纹后灌入原时间线；
- 本地硬保护和 `validate_selections` 始终拥有最终否决权；
- 上下文审查与投影计划可保存但不修改 Codex；原任务应用不可用，派生投影当前真实 round-trip 失败并保持上游阻塞；
- Hook 成功保存计划时同步创建 `PendingTrimPlan`；
- 待处理页重新检查计划 SHA、内容指纹、能力指纹、有效期和任务状态，只有 `READY` 才能打开原 GUI；
- 当前不运行 `trim apply`，不把目标创建、`{}` 响应或方法存在标记为投影成功；只有新的完整 probe 通过后才可重新研究 `APPLIED`。

当前上下文能力边界：审查/投影计划可用；应用到原任务不可用；派生投影 `blocked_upstream`；敏感信息确定性 `Replace/Redact/Protect` 是后续优先方向。2.5 永久删除继续按独立门禁验收。

### 记忆管理 MVP

- 只管理用户明确登记的本地 UTF-8 Markdown/文本文件；
- 拒绝路径逃逸、符号链接和未登记路径；指令文件需要独立开关；
- 按 front matter、标题、列表项、段落、代码块和结构空白分段；
- 支持 `KEEP/DELETE/REPLACE/PROTECT`；
- LLM 建议绑定当前 segment ID 与内容 SHA-256，并受到本地结构保护否决；
- 原 GUI 展示完整 unified diff，用户再次确认后创建私有版本、复核并发漂移、原子替换并重读验证；
- 支持不可变恢复计划、恢复前安全备份和审计。

### MCP 与验收

- 独立 Streamable HTTP MCP 服务，默认 Bearer token、精确 Origin 和请求大小门禁；
- 只暴露盘点、建议准备、打开审查和状态查询，不暴露归档、裁剪应用、记忆写入或永久删除执行器；
- `csm acceptance run` 在隔离目录中执行 MCP 白名单、记忆写入/恢复回环、待处理计划生命周期和原 GUI 记忆模式检查；
- `csm acceptance release` 额外要求 age 和稳定安装包；
- `scripts/accept_first_delivery.sh` 串联完整源码门禁、自动报告和可选 macOS bundle 验收。

## 自动化证据的含义

通过 `scripts/check.sh` 和 `csm acceptance run` 只能证明：

- 当前源码格式、静态类型、单元/集成/offscreen GUI 测试和 Skill 合约通过；
- 隔离临时文件中的记忆修改、恢复、审计和待处理状态机闭环通过；
- MCP 工具面没有越出已审查白名单。

它们不能替代：

- 真实 ChatGPT 自定义 MCP app 与固定 Tunnel 联调；
- 真实 Codex 账号和实体 Cocoa 窗口输入；
- Apple Developer ID 签名、公证和 staple；
- Windows x64 实体环境、AuthentiCode 和 SmartScreen；
- 公开发布或生产环境验收。

## 2026-08-18 本地候选证据

在真实 Apple Silicon macOS 上，从当前 checkout 完成 fresh 构建与验收：

- 版本：`1.1.0`；
- 通道：`macos-test-adhoc`；
- 架构：`arm64`；
- PySide6 / Qt：`6.11.1`；
- bundle 内 age：`v1.3.1`；
- age SHA-256：`0e3ea0b1bed2b30aa2dc46eef4e1723864d626c80f37319c20d9b73ca045f56f`；
- 全量自动测试：`221 passed`，只有第三方弃用/字体别名提示，无失败和跳过；
- source acceptance：`delivery_ready: true`；
- release acceptance：6 个必需检查全部 `passed`；
- release report SHA-256：`4491461e40c355b83b0682457610d848473226876fb57445f9b6d519c2a33207`；
- 测试 ZIP：`CodexSessionManager-macos-arm64-v1.1.0-test.zip`；
- ZIP SHA-256：`630c21c806bc84c58dad9b9f398d25be035309864c603df85e453f2e0249f6fa`；
- 原 `.app` 和从 ZIP 解压的 `.app` 均通过 bundle doctor、Hook fail-open smoke、offscreen GUI smoke 和 ad-hoc codesign 校验。

本地 evidence 与发布资产位于忽略的 `build/`、`dist/` 和 `artifacts/` 目录，不作为源码提交的一部分。该测试包未经 Apple 公证，不应当作公开生产发布包。

## 仍需人工关闭的交付门禁

1. 使用一个无重要内容的真实 Codex 测试任务验证对话清理、标题修改和源任务保护；
2. 使用一个专用测试 `MEMORY.md` 在实体 Cocoa 窗口中验证 diff、版本、恢复和并发漂移；
3. 在固定 Cloudflare Tunnel 与真实 ChatGPT 工作区中验证 MCP 认证、工具发现、窗口唤起和断线恢复；
4. 继续满足 2.5 永久删除的用户主动单选、独立单根计划、CSM 可信归档证据、archive-bound 当前备份、进程复核和双重确认门禁；按 ADR 0010 不再设置固定等待期；
5. 首批用户测试前记录安装、Gatekeeper 提示、回退和问题收集流程；
6. 正式公开发布前完成 Developer ID、公证、Windows 实机验收、checksum 登记和对应平台签名。

2.4 上下文应用不属于仍待关闭的交付能力；其 `CLOSED_WITH_UPSTREAM_BLOCKER` 结论和真实 round-trip 失败必须在验收报告与发布说明中保留。

完整步骤见 [`v1.1.0 首次交付验收 Runbook`](../acceptance/first-delivery-v1.1.0.md)。
